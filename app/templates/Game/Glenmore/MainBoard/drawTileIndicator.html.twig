{#
This section displays the current draw tile and the number of remaining tiles in it
@param : currentDrawTile
@return : display the current draw tile and the number of remaining tiles
#}
<div class="flex justify-center relative items-center h-full w-fit">
    <img
            class="relative aspect-auto max-h-full"
            src="{{ asset('resourcesGames/Glenmore/CardsLevel/cardLevel' ~ currentDrawTile.level ~ '.svg')
            }}"
            alt="{{ currentDrawTile.level }}"
    />
    <span
            class="absolute inline-flex items-center justify-center w-6 h-6 text-xs
                   -right-3 font-bold text-white
                                           bg-red-500 border-2 border-white rounded-full top-0 end-0 dark:border-gray-900"
    >
        {{ currentDrawTile.tiles|length }}
    </span>
</div>