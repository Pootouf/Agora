input {
  file {
    type => "mercure_access"
    path => "/var/log/mercure/mercure-access.log"
    start_position => beginning
  }
}


output {
  elasticsearch {
    hosts => "http://elasticsearch:9200"
    index => "logstash-mercure-%{+YYYY.MM.dd}"
    user => "elastic"
    password => "changeme"
  }
}